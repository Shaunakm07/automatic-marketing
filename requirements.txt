anthropic>=0.40.0
supabase>=2.4.0
httpx>=0.27.0
python-dotenv>=1.0.0
PyGithub>=2.3.0
pydantic>=2.7.0
rich>=13.7.0
tenacity>=8.3.0

# Dashboard / API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Trend agent
tweepy>=4.14.0          # X/Twitter API v2 (optional — only needed if X_BEARER_TOKEN is set)

# Slideshow image generation
huggingface_hub>=0.23.0
Pillow>=10.0.0
